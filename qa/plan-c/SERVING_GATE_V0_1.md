# Plan C v0.1 serving and evaluation cost gate — 2026-09-24

**Decision: NOT APPROVABLE YET for a paid endpoint.** The one approved SFT LoRA job `ftjob-48927a40e28f47ba811ed3e2ac9845d6` succeeded. Its [checkpoint and billing evidence](PILOT_V0_1.md) do not prove that the adapter is deployable on a particular GPU/template or establish the exact all-in cost. No endpoint, additional training, inference, Writer override, Production promotion, or Phase 2 work has been performed.

## Serving routes inspected

| Route | Evidence on 2026-09-24 | Decision / missing cost |
| --- | --- | --- |
| Nebius Public endpoint for this fine-tuned LoRA | Account's Public endpoints did not list the tuned model. Current [post-training model list](https://docs.tokenfactory.nebius.com/post-training/models) says deployment of fine-tuned models is **Dedicated only**. An older `/fine-tuning/deploy-custom-model` search result advertises serverless adapter hosting, but the current linked page returned 404 and the signed-in job's Actions menu offered only **Job details** and **Download model files**. | Do not assume a per-token tuned endpoint exists. The public `openai/gpt-oss-20b` and exact `unsloth/gpt-oss-20b-BF16` base model pages currently show **Public endpoint: Not available**; the generic catalog list badges are misleading here. |
| Nebius Dedicated custom weights | [Dedicated FAQ](https://docs.tokenfactory.nebius.com/ai-models-inference/dedicated-endpoints/faq): custom fine-tuned weights are beta **available on request**; Support must enable and confirm a compatible setup. Signed-in self-service form did not return `inkrya-writer-v0-1` when searching the model selector. | **Actual checkpoint's GPU count/type, supported BF16 vs merged/quantized format, template, replica constraints, availability, support/setup fees and quote are unknown.** Do not Create/Continue to deployment as a substitute for those answers. |
| Downloaded adapter with local/private inference | The provider exposed six checkpoint files including `adapter_model.safetensors` (~8 MB), `adapter_config.json`, tokenizer and template, but no base weight shards; see [manifest](PILOT_V0_1.md). [Nebius's MoE guide](https://docs.tokenfactory.nebius.com/post-training/merge-moe-lora-weights) describes obtaining the **exact** `unsloth/gpt-oss-20b-BF16` base and merging the adapter for vLLM. | Technically documented route without a Token Factory Dedicated endpoint, contingent on compatible private compute. This workspace has no visible NVIDIA GPU; base BF16 weights, GPU, storage, download, merge and running time must be priced separately. Do not publish weights to GitHub or an unintended Hugging Face repo. |
| Nebius Batch / other managed inference | Generic [Batch Inference](https://docs.tokenfactory.nebius.com/data-lab/batch-inference) accepts a model ID, but no evidence that this private SFT checkpoint can be selected. | **Not a verified serving route for this checkpoint**. Do not launch a batch job to probe it. |

The exact training base is **BF16** (`unsloth/gpt-oss-20b-BF16`); the self-service **base** `openai/gpt-oss-20b` template visible in the account is **FP4, Base flavor, 1× H100 `gpu-h100-sxm` in `eu-north1`, one replica**, with a displayed approximate **$0.0675 per GPU/replica per minute = $4.05/hour**. This only prices that *public base-model template*. There is no proof that the trained BF16 LoRA or merged BF16 artifact can be loaded on it without conversion or Support enablement. L40S's lower GPU rate is not a verified compatible template.

## Billing mechanics and conditional arithmetic

The [Dedicated overview](https://docs.tokenfactory.nebius.com/ai-models-inference/dedicated-endpoints/overview) says per-GPU-hour billing with **per-minute granularity**. The [billing policy](https://docs.tokenfactory.nebius.com/ai-models-inference/dedicated-endpoints/billing-policy) says provisioning/not-ready is not billed, ready with running replicas is billed, and stopping with zero replicas is not billed. The [operations guide](https://docs.tokenfactory.nebius.com/ai-models-inference/dedicated-endpoints/operating) allows `enabled=false` to stop, while the [capacity guidance](https://docs.tokenfactory.nebius.com/ai-models-inference/dedicated-endpoints/capacity-and-scaling) warns that restarting releases and re-requests shared GPUs. No guaranteed model-loading/provisioning time, explicit minimum charge duration, account-specific custom-weights setup fee or storage/merge charge was established. The first charged minute would be **$0.0675 only under the base-H100-rate assumption**, not an exact minimum for this LoRA.

| Proposed bounded *active* window | LoRA candidate | Exact BF16 base comparator, if separately hosted | Conditional sum at one H100 each |
| --- | ---: | ---: | ---: |
| 2 held-out canaries | 15 min = $1.0125 | 15 min = $1.0125 | $2.0250 |
| All 30 held-out cases, including canary IDs | 60 min = $4.0500 | 60 min = $4.0500 | $8.1000 |
| Four legacy golden cases | 15 min = $1.0125 | 15 min = $1.0125 | $2.0250 |
| Candidate repeatability after human review | 15 min = $1.0125 | None planned | $1.0125 |
| **Total planning window** | **105 min = $7.0875** | **90 min = $6.0750** | **195 min = $13.1625** |

These are **illustrative arithmetic, not the exact estimated evaluation charge, a service-level commitment, or a provider-enforced cap**. They assume the same 1×H100 base rate can serve the fine-tuned BF16 weights *and* exact BF16 base, every stage fits the stated active minutes, and no further billable setup, storage, taxes, additional replicas or idle review time. None of those assumptions has been confirmed. Starting/stopping between stages may save active time but can lose GPU capacity; human review should occur offline with the endpoint stopped. A local BF16 base comparator could replace a paid second endpoint **if** compatible compute is available, but its actual cost has not been priced. Quantized `openai/gpt-oss-20b` is a proxy, **not** the identical BF16 base for a clean before/after inference comparison.

To produce an approval-ready **exact estimate**, obtain from Nebius the checkpoint's accepted artifact type (adapter vs merged), exact template/model ID and quantization, GPU type/count, per-minute price, minimum/rounding rule, charges for custom enablement/merge/storage, whether disabling immediately ends billing, and any realistic provisioning expectation. Confirm a route and cost for the identical BF16 base comparator. Then compute `billable minutes × verified price × replica/GPU count + documented extras` and set an explicit active-time cap. Stop if the quote materially differs from this scenario. **Do not create an endpoint until the user accepts the actual cost gate.**

## Evaluation execution after a separate serving approval

1. Keep all references sealed. Use only `qa/plan-c/heldout.records.jsonl` / `heldout.reference.jsonl` and the four cases in `scripts/phase1-writer-cases.mjs`; use each held-out conversational record's `messages` **without its final reference assistant answer**. For the canary select `pc-09-1-tenang` and `pc-10-1-tegang`, which come from different held-out settings and tones. Neither appears in training.
2. Run each canary with the **same** input constraints, prompt, max output budget, temperature and seed on the tuned adapter and *identical* BF16 base. Record the exact model/adapter/checkpoint IDs, generation settings, inference tokens, latency, timestamps, and raw output separately in private evaluation evidence. Do not invoke a third model as an unnoticed fallback.
3. Apply the existing deterministic checks for word range, requested paragraph count, meta/incomplete output and obvious irrelevant foreign leakage. Blind human review must check natural Indonesian, coherence, required facts, forbidden changes, newly invented actions/facts, diction, POV and paragraph adherence. **If either canary is unpromising, stop** and review before spending on the remaining cases; do not weaken validators.
4. Only if both canaries are promising, run the **30 held-out cases in total** for both arms with the same rubric and preserve per-case pass/fail and raw outputs. Stop the endpoint before manual review. If acceptable, run the four untouched legacy golden continue/rewrite cases and human review them; the final writer gate requires **4/4** mechanical **and 4/4** human acceptance.
5. If the candidate passes, repeat the four legacy cases with the same configured LoRA route and evaluate the same rubric. On success run tests, typecheck, build and relevant HTTP checks. Any serving route integrated into the app must log explicit role/provider/model/checkpoint without silent fallback. Only then consider Phase 1 COMPLETE; Production remains Gateway and Phase 2 remains blocked until that gate is honestly met.

Training loss `1.6433256` and success do **not** measure prose quality. This document authorizes no inference or deployment.

## Follow-up: Token Factory versus private Nebius AI Cloud — 2026-09-24

**Neither route has been launched.** The account's Model endpoints screen still lists **Dedicated 0 / Public 24**; the only SFT LoRA job remains Succeeded, with no Deploy action or public endpoint for its name. The [current model list](https://docs.tokenfactory.nebius.com/post-training/models) says fine-tuned deployment is Dedicated-only, while the [Dedicated FAQ](https://docs.tokenfactory.nebius.com/ai-models-inference/dedicated-endpoints/faq) says custom fine-tuned weights need beta access on request. An older search-indexed serverless-LoRA document is not evidence of current account availability. Token Factory's **Contact us** dialog directs support requests to `tokenfactory-support@nebius.com` via a `mailto:` link; it offers no in-console support form. An outbound email channel is not connected to this workspace, so **no Support request was sent**. This is the minimum-disclosure inquiry to send through an authorized email channel, without dataset, manuscript, account balance or credentials:

> Subject: Serving and cost quote for existing GPT-OSS 20B BF16 SFT LoRA checkpoint
>
> Hello Nebius Token Factory Support, our existing successful SFT LoRA checkpoint uses `unsloth/gpt-oss-20b-BF16` and an approximately 8 MB PEFT attention adapter. We plan only a bounded synthetic evaluation, without production traffic. Is serving this checkpoint on a shared/serverless endpoint supported **in our current account**, and if so what is the exact per-token pricing and minimum? If not, is a Dedicated Endpoint the only supported route, and do we need custom-weights beta approval or to merge/quantize the adapter? Please identify the smallest **compatible** model template, GPU type/count, region and replicas; price per billable unit, billing start/stop and rounding or minimum duration; any setup, merge, storage or onboarding charge; typical provisioning/model-loading time; and whether disabling the endpoint immediately stops GPU billing and later restart remains available. Can the **identical BF16 base** be evaluated on the same route for an unbiased before/after comparison? Please provide an account-specific quote before any resource is created.

### Private AI Cloud feasibility and account boundary

The downloaded `adapter_config.json` names exactly `unsloth/gpt-oss-20b-BF16`, PEFT LORA, rank 8, alpha 8, dropout 0.05 and targets `q_proj`, `k_proj`, `v_proj`, `o_proj`. Read-only inspection of the adapter's safetensors header found **192 BF16 tensors**, paired `lora_A`/`lora_B` for each of four attention targets in all **24 layers**, matching the maintainer's [24-layer base configuration](https://huggingface.co/unsloth/gpt-oss-20b-BF16/blob/main/config.json), and zero unmatched names. The adapter config does **not** pin a base-model commit (`revision: null`); before evaluation, record the exact Hugging Face base revision and weight hashes, compare tokenizer/chat-template hashes with the training artifacts, and verify outputs with adapter enabled and disabled. Both arms can then use the same pinned downloaded base, but a bitwise match to Nebius's undisclosed training snapshot cannot be proven from this adapter alone. The maintainer's [base weight index](https://huggingface.co/unsloth/gpt-oss-20b-BF16/blob/main/model.safetensors.index.json) reports **41,829,514,368 bytes = 38.957 GiB** of BF16 weights; the LoRA file adds **7,984,752 bytes = 0.0074 GiB**. This establishes **structural compatibility, not a successful runtime load**.

[vLLM's model matrix](https://docs.vllm.ai/en/v0.29.0/models/supported_models/) marks GPT-OSS LoRA supported, and its [LoRA serving guide](https://docs.vllm.ai/en/latest/features/lora/) describes one server exposing the base and adapter as separate model IDs. [Nebius's MoE merging guide](https://docs.tokenfactory.nebius.com/post-training/merge-moe-lora-weights) explicitly uses this BF16 base and a downloaded PEFT adapter to construct a merged checkpoint for vLLM if direct adapter loading fails. No GPU was started to validate either path. A single H100 with **advertised 80 GB VRAM** leaves nominally **~35–41 GiB before runtime/KV/cache allocations**, depending on GB versus GiB interpretation and reserved memory; choose a short `max_model_len` appropriate for the synthetic 8K training context and small concurrency, measure actual free VRAM at load time, and fail closed if loading fails. Avoid automatically quantizing the base or switching to FP4: that would change the baseline. A **single on-demand** `gpu-h100-sxm`, `1gpu-16vcpu-200gb` VM in `eu-north1` (1 H100, 16 vCPUs, 200 GiB host RAM) is a [published Nebius preset](https://docs.nebius.com/compute/virtual-machines/types). This is **not** an 8-GPU cluster.

The AI Cloud console showed a site-served Cloudflare security block in this browser even after one reload. Consequently **project access, 1-H100 capacity/quota, account-specific credits, and VM creation-form price are unverified**. The Token Factory trial balance cannot be assumed to pay AI Cloud VM charges. Do not try alternate console routes to evade the block. Before an approved GPU launch, independently verify those four account facts and stop if the VM price or quota differs materially from the published plan.

### On-demand cost estimate, no resource created

As of **2026-09-24**, Nebius lists the single H100 preset at **$3.85 per GPU-hour including its bundled CPU/RAM** in `eu-north1`, changing to **$4.50 per GPU-hour effective 2026-10-01** ([AI Cloud prices](https://nebius.com/prices), [Compute rate](https://docs.nebius.com/compute/resources/pricing)). GPU VM usage is [billed per second and stopped VM compute is unbilled](https://docs.nebius.com/compute/resources/pricing). Plan a **200 GiB regular network SSD** boot/work disk, with room for the ~42 GB base, optional ~42 GB merged output and runtime packages; its listed rate is **$0.071/GiB per 730 hours** and it accrues until deleted even while the VM is stopped. For **24 hours of disk retention**, the estimated storage charge is `200 × 0.071 × 24 / 730 = $0.46685`. Confirm image compatibility, actual boot-disk size and offered disk type before any launch.

| Bounded one-H100 scenario | GPU today at $3.85/h | 200 GiB SSD held 24 h | Estimated subtotal today | GPU from Oct 1 at $4.50/h | Estimated subtotal from Oct 1 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2 canaries only; up to 1 h including download/load | $3.85 | $0.47 | **$4.32** | $4.50 | **$4.97** |
| Complete two-arm evaluation; 4 h cumulative active VM time | $15.40 | $0.47 | **$15.87** | $18.00 | **$18.47** |
| Conservative spending ceiling; 6 h cumulative active VM time | $23.10 | $0.47 | **$23.57** | $27.00 | **$27.47** |

These are **estimates, not a charge quote or an assurance that the job will fit in four hours**. The 6-hour line accommodates slower base download, LoRA-load preflight, stage restarts and sequential base/adapter inference; stop the VM on reaching the approved time/spend cap. Disk beyond 24 hours adds approximately **$0.47/day** until deleted. No public IP or network transfer line is included because Nebius's [published price list](https://nebius.com/prices) currently lists those as free; tax, account-specific terms, alternative disk configurations, quota, availability and possible software download restrictions remain unverified. After approval, read the account VM creation quote **before** creating it; stop if it changes the estimated ceiling. Never assume a Token Factory trial credit is transferable to AI Cloud.

The same public [AI Cloud pricing FAQ](https://nebius.com/prices) says the **minimum first payment is $25**. This is a potential funding requirement, **not** a $25 minimum VM usage charge. Because the AI Cloud account could not be inspected, its payment status and usable credits are unknown; confirm both before asking anyone to fund or create the VM. A new payment, tax and account terms can make the cash outlay differ from the usage estimate above.

### Execution contract after a separate compute approval

1. Pin the exact public BF16 base revision and record each weight/config/tokenizer hash. Download the same checkpoint adapter privately, verify the recorded SHA-256, and keep both model directories off the public repo. On the one-H100 VM, preflight vLLM's model list with base plus **named** LoRA; verify the adapter really activates. If unsupported, use the documented **exact-BF16** merge and reload the base and merged model sequentially on this same GPU. Record any failure and its runtime/memory evidence; do not add GPUs automatically.
2. Keep the OpenAI-compatible server bound to loopback or private networking. Feed the two sealed held-out canaries (`pc-09-1-tenang`, `pc-10-1-tegang`) to the base and adapter with the same messages minus reference answer, tokenizer, system/developer template, temperature, seed, `max_tokens` and bounded context. Record full outputs, role/model ID, base revision, checkpoint hash, latency and token usage. Explicitly distinguish base from tuned routes; no silent fallback.
3. Shut down the VM after the canary and review outputs offline. If **both** are promising under the unchanged word-range, paragraph, POV, fact, forbidden-change, diction, language and naturalness rubric, restart for the **30 held-out in total** (including both canary IDs under the same full-run settings) and compare both arms. Stop and review; then run the four untouched golden cases and blind human review. Repeat the four golden cases only after a 4/4 mechanical and 4/4 human pass. Persist raw synthetic outputs and per-case human judgments outside any public weights store before terminating compute.
4. Monitor elapsed **running** GPU seconds and disk lifetime; stop compute immediately between review stages, then delete the VM and disk after verified artifact export. Test whether stop releases capacity and whether the boot disk remains present; do not rely on guest `poweroff` alone, which can trigger recovery. If quality and repeatability pass, run code regression before any separate Writer routing decision. Phase 1 stays INCOMPLETE until those gates pass; Production stays Gateway and Phase 2 remains blocked.
